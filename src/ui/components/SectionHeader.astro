---
type Props = {
  icon: string;
  title: string;
};

const {icon, title} = Astro.props;
---

<div class="flex flex-row gap-4 items-center w-full">
  <span class={`${icon} w-9 h-9`}></span>
  <h3 class="text-3xl font-medium">{title}</h3>
</div>
