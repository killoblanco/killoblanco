---
import { buttonVariants } from "@/ui/elements/button";
import BaseLayout from "@/ui/layouts/base.astro";
import { cn } from "@/ui/utils";
---

<BaseLayout>
  <main class="container max-w-screen-xl">
    <section class="grid items-center justify-items-center min-h-dvh">
      <div class="flex flex-col gap-8">
        <div class="inline-flex items-center gap-8">
          <h1
            class="scroll-m-20 text-4xl font-black tracking-tight lg:text-5xl"
          >
            404
          </h1>
          <hr class="border-r-2 border-r-border h-12" />
          <p>No se pudo encontrar esta página.</p>
        </div>
        <a href="/" class={cn(buttonVariants({size:'lg'}), 'mx-auto')}>
          <span class="icon-[tabler--home] size-5 mr-3" />
          Volver al inicio
        </a>
      </div>
    </section>
  </main>
</BaseLayout>
